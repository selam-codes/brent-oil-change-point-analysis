# Scripts

Command-line utilities for reproducing analysis steps outside the notebooks (data prep, running the change point model in batch, exporting model outputs consumed by the dashboard backend).
